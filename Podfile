# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'network-abstraction-layer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire'
  pod 'PromiseKit'

  # Pods for network-abstraction-layer

  target 'network-abstraction-layerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'network-abstraction-layerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
